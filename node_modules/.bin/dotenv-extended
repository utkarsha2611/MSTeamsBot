#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../dotenv-extended/lib/bin/index.js" "$@"
  ret=$?
else 
  node  "$basedir/../dotenv-extended/lib/bin/index.js" "$@"
  ret=$?
fi
exit $ret
